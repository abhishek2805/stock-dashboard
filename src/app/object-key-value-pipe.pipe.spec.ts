import { ObjectKeyValuePipePipe } from './object-key-value-pipe.pipe';

describe('ObjectKeyValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectKeyValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
